# Runtime

`runtime/` 用于承接仓库运行过程中产生的非源码输出。

## 当前内容

- `logs/`：运行日志、调试输出和其他过程日志
- `pids/`：`run/services.py` 维护的当前服务 PID 文件
- `tmp/`：运行时临时目录预留位置

## 当前边界

- 这里存放运行产物，不放源码、配置或长期业务数据
- 需要长期保留的图、文档和辅助资料应放在 `artifacts/`
- 业务数据库和采集结果应放在 `data/`