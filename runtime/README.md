# Runtime

`runtime/` 用于承载运行时产物，不放源码，也不作为长期数据目录。

## 当前结构

- `logs/`：服务运行日志输出目录
- `pids/`：由 `run/services.py` 维护的服务 PID 文件
- `tmp/`：临时文件与一次性产物目录，测试临时文件统一放在 `runtime/tmp/tests/`

## 使用约束

- 不要把运行日志、PID 文件或临时产物重新写回仓库根目录
- 不要把流程图、文档或长期资料放进这里；这类内容应进入 `artifacts/`
- 不要把数据库文件放进这里；运行数据库固定为 `data/app.db`
