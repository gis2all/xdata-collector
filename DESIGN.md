# XData Collector UI DESIGN.md

## 设计目标与适用范围

这份 `DESIGN.md` 是当前仓库的 UI 设计规范入口，供人和 AI 在做前端设计、页面改版、组件视觉调整和布局重构时统一遵循。

目标是把界面收口成适合数据工作台的扁平、高密度、低装饰语言：信息先于造型，数据线先于容器线，操作效率先于“高级感”。

这份规范服务以下页面：

- `运行总览`
- `手动搜索`
- `自动任务`
- `结果浏览`
- `运行日志`
- `设置`

同时约束全局导航、页面头部、卡片、表单、按钮、表格、状态提示和空状态的表达方式。

---

## 视觉气质与整体原则

### 目标气质

- 干净、克制、秩序明确
- 工具感强，但不生硬
- 信息密度中等偏高，优先保证效率
- 操作路径清晰，避免页面像“能力堆放仓库”
- 视觉重点少而准，不用过多颜色抢注意力

### 核心原则

- 一个页面先回答三件事：当前是什么、现在能做什么、下一步最重要的动作是什么
- 一个区域只承载一种主要语义，不把状态、编辑、导入、危险动作混在同一层
- 主按钮稀缺。首屏尽量只有一个真正的主操作，其余动作退成次级或辅助
- 大分区用于建立页面秩序，普通内容不再依赖卡片套卡片
- 表格优先扫描效率，不能为了“好看”牺牲对比和批量操作能力
- 页面先建立层级，再考虑装饰

### 明确拒绝

- 营销页式 hero
- 大面积装饰渐变
- 过度玻璃拟态
- 大量悬浮阴影和彩色描边
- 每块内容都做成抢眼卡片
- 为了“高级感”牺牲工作台效率

---

## 主题与颜色

### 默认深色主题

工作台默认使用深色主题；浅色主题作为可选切换。所有颜色必须走语义 token，禁止在组件里写死色值。

### 深色主题 token

```css
:root {
  --workbench-surface: #131613;
  --workbench-surface-soft: #1a1d1a;
  --workbench-surface-muted: #1a1d1a;
  --workbench-border: #303530;
  --workbench-border-strong: #5a6058;
  --workbench-text: #ecede8;
  --workbench-text-muted: #969b91;
  --workbench-title: #ecede8;
  --workbench-accent: #d9ff3f;
  --workbench-accent-soft: rgba(217, 255, 63, 0.12);
  --workbench-accent-text: #192100;
  --workbench-success-text: #d9ff3f;
  --workbench-danger-text: #df735d;
  --workbench-warn-text: #e6c35c;
  --workbench-focus-ring: 0 0 0 3px rgba(217, 255, 63, 0.25);
}
```

页面背景为 `#0d0f0d`。

### 浅色主题 token

```css
html[data-theme="light"] {
  --workbench-surface: #f7f6ef;
  --workbench-surface-soft: #e7e6dd;
  --workbench-surface-muted: #e7e6dd;
  --workbench-border: #cccec3;
  --workbench-border-strong: #8f9589;
  --workbench-text: #20231f;
  --workbench-text-muted: #6d746b;
  --workbench-title: #20231f;
  --workbench-accent: #738d10;
  --workbench-accent-soft: rgba(115, 141, 16, 0.12);
  --workbench-accent-text: #f5f7e8;
  --workbench-success-text: #738d10;
  --workbench-danger-text: #b54e38;
  --workbench-warn-text: #966e00;
  --workbench-focus-ring: 0 0 0 3px rgba(115, 141, 16, 0.25);
}
```

页面背景为 `#efeee7`。

### 颜色角色与使用边界

- `accent`（荧光绿 / 深绿）：主操作按钮、当前导航指示、焦点态、checkbox / radio、进度条、成功状态。不要把 accent 同时拿去表示“主按钮 + badge + 卡片背景 + 大标题强调”。
- `text` / `title`：正文和标题。
- `text-muted`：说明、次级文字、表头、标签。
- `success`：成功提示，与 accent 同色系表达（浅底 + 语义字）。
- `danger`：删除、彻底删除、错误、失败。
- `warning`：中性警告。
- 状态色优先以浅底 + 语义字出现，不默认用纯色整块铺满。

---

## 字体与文本规则

### 字体栈

全站只使用一种字体族，禁止在页面里混排其他字体（包括等宽字体）：

```css
font-family: Inter, "Noto Sans SC", "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
```

- 西文使用 `Inter`，中文使用 `Noto Sans SC`
- 字体加载失败时回退系统无衬线字体
- 代码 / 日志 / JSON 预览等文本块也使用同一字体族，不引入 monospace

### 字级层级

- 页面标题：20px / 600 / text
- 区块标题：17px / 600 / text
- 子区标题：15px / 600 / text
- 大数字：22px / 700 / text
- 数据 / 表格正文：13px / 400 / text
- 正文：12px / 400 / text
- 字段 / 组标签：12px / 600 / muted
- 说明 / 次级：12px / 400 / muted
- 表头：10px / 600 / muted / uppercase
- eyebrow / 徽章：10px / 400 / muted / uppercase
- 导航：12px / 500 / muted
- 按钮：12px / 700（主按钮用 accent，次级按钮 12px / 600 / muted）

### 文本原则

- 标题负责结构，不负责抒情
- 页面说明保持短句，优先解释“这页做什么”
- 次级说明只写必要上下文，不写长教程
- 表单标签尽量直接，少用模糊词
- 一个区块里不要同时出现多个强标题层级
- 界面文案统一使用中文，不使用英文 eyebrow 或英文标题

---

## 间距、边界与层级

### 间距节奏

使用细颗粒节奏控制工作台密度：

- 4 / 8 / 12 / 16 / 20 / 24 / 32

使用建议：

- 行内控件间距：8
- 小区块内边距：12
- 普通卡片内边距：16
- 页面主区块间距：16-20
- 大模块之间：24-32

### 圆角、边框与阴影

- 默认无圆角：容器、表格外壳、详情轨、表单控件、按钮和 badge 默认 `border-radius: 0`
- 默认无装饰阴影：普通工作区、工具区、摘要区和空状态不使用阴影
- 只保留必要边界：表格外壳、表单控件、危险操作区、JSON / 日志代码块可以有明确边界
- 只留数据线：表格行、表头和 key-value 详情行之间可以有轻量线，标题线、工具条线、空状态边线默认删除

### 层级表达顺序

层级优先通过这些方式表达，按顺序从强到弱使用：

1. 信息顺序
2. 标题层级
3. 留白与分区
4. 背景深浅
5. 必要数据线
6. 必要边界
7. 强调色

---

## 控件规范

### 按钮

- 主按钮：accent 填充 + `accent-text` 深色文字，仅用于当前最重要动作
- 次级按钮：描边 + 弱化文字，承载普通操作
- 危险按钮：仅用于删除、彻底删除等不可逆行为
- Ghost 按钮：仅用于不应抢视觉的辅助动作

规则：

- 同一操作区尽量只有一个主按钮
- 当一个区域已经有“保存”主按钮时，导入、重置、刷新都应退为次级
- “立即运行”“保存任务”“保存到当前任务包”这类动作不要在一个视图里同时都做主按钮

### 输入框与选择器

- 默认 `surface-soft` 背景，细边框
- 输入框、下拉框统一最小高度（42px），不要一页里出现多种高度体系
- 焦点态使用绿色 2px outline，`outline-offset: 0`，必须与控件边缘完全贴合
- 下拉框箭头使用主题色 SVG，去掉浏览器默认样式
- 错误态优先红色边框 + 短错误文本，不用整块大红底
- checkbox / radio 使用绿色 `accent-color`

### 卡片

- 卡片只用于分区，不用于给每一条内容套壳
- 卡片内部必须有清晰标题或分区目的
- 卡片之间靠留白建立关系，不靠强彩色背景

### 表格

- 保持紧凑但可读
- 表头清晰、排序 / 筛选可见、列宽可控
- 行操作优先收口，不要一行堆太多抢眼按钮
- 批量操作属于表格上方的管理条，不要塞回行内

### Badge 与状态提示

- Badge 只承担状态摘要，不承担主要操作
- 尽量短：2-4 个字
- 同一视图里 badge 数量受控，避免彩色标签墙

### 工具栏

- 工具栏要分“筛选工具”和“主操作”
- 如果既有查询、筛选、批量管理，又有新建或保存，主操作必须视觉更稳、更靠右或更靠前
- 工具栏过长时，应拆成两层，而不是所有控件同排

### 分页、空状态、提示

- 分页区域保持稳定位置，优先靠近表格顶部，避免长表格必须滚到底才能翻页
- 空状态要说明“当前没有什么”以及“下一步能做什么”
- 成功提示短而明确，错误提示直接指出问题，不写泛化文案

---

## 品牌与文案

- 产品名：`X 数据采集器`
- 界面文案统一使用中文
- 侧栏品牌区只保留一个 `X 数据采集器` 标题，不显示重复副标题
- 浏览器 favicon：`web-ui/public/favicon.svg`，绿色（`#d9ff3f`）X 图标
- 页面头部不使用英文 eyebrow，不重复描述性说明文字

---

## 页面布局原则

### 全局导航

- 左侧固定 rail 导航，保持稳重、克制、低干扰
- 当前页高亮要清楚，但不要过度发光或高饱和：用左侧绿色竖线 + 文字权重表达
- 导航标题是产品名，不承担品牌宣传
- 右侧工作区铺满 rail 之外的可用宽度，不做无意义居中收窄
- 导航支持 hash 深链：`#/dashboard`、`#/manual`、`#/jobs`、`#/results`、`#/logs`、`#/settings`；非法 hash 回到 Dashboard，`localStorage` 只作为兜底

### 页面头部

每个页面头部都包含：

1. 页面标题（20px / 600）
2. 当前页最重要的主操作（右侧）

页面头部不承载英文 eyebrow 和重复说明文字。主操作默认位于右侧，并在头部区块内垂直居中对齐。

### 页面内部结构

页面结构尽量遵循：

1. 当前对象或当前状态
2. 主操作区
3. 主要内容区
4. 次级详情或历史信息

如果页面信息很多，优先拆成少数几个稳定大区，而不是堆很多大小接近的小卡片。

---

## 页面地图

| 导航名 | 路由 | 职责 |
| --- | --- | --- |
| 运行总览 | `#/dashboard` | DB / X 健康状态、最近校验摘要 |
| 手动搜索 | `#/manual` | 任务包草稿编辑、预览、执行、导入导出 |
| 自动任务 | `#/jobs` | 调度任务列表、绑定任务包、批量操作 |
| 结果浏览 | `#/results` | raw / curated 切换、筛选、排序、分页、详情 |
| 运行日志 | `#/logs` | 运行记录和当前服务日志快照 |
| 设置 | `#/settings` | 只维护 `config/workspace.json` 与会话 token |

---

## 重点页面规则

### 自动任务

目标：让这页首先像任务管理工作台，其次才像表单集合页。

规则：

- 保留双栏布局
- 左侧继续使用表格列表，承担搜索、筛选、批量管理、分页和任务切换
- 左侧列表区优先拆成“筛选层”和“表格管理层”，不要把页级动作和表级动作混在一起
- 右侧是统一的“当前任务工作区”，默认按可编辑工作区设计，不强调 `view/edit` 切换
- 右侧阅读顺序固定为：当前任务状态摘要、主保存操作、调度设置、任务包操作、任务正文摘要、搜索条件、规则、运行统计或历史
- 顶部主保存入口必须始终可达
- 任务包身份信息优先收成轻提示，不再单独展开成“当前绑定任务包”大卡
- 任务包相关操作要聚合，不要散落到规则编辑器内部

### 手动搜索

目标：让用户一眼理解“当前在编辑并执行一个任务包草稿”。

规则：

- 顶部只保留标题、短说明和唯一主按钮“立即执行任务”
- 左侧编辑区固定顺序为：任务包操作、任务正文摘要、搜索条件、规则
- 右侧保留“执行上下文”轨，只展示草稿状态和最近一次执行摘要，不承载第二套主操作
- 执行按钮必须在首屏清晰可见
- 执行结果区与编辑区明显分层，避免互相抢视线
- 结果摘要优先呈现查询、命中数、规则摘要，再看明细表

### 结果浏览

目标：像高效数据工作台，而不是普通内容列表页。

规则：

- 顶部先呈现当前表、筛选和主操作
- 表格是绝对主角，列控制、排序、列宽拖拽都属于辅助能力
- `raw` / `curated` 切换要明确且稳定
- 批量删除、去重、字段控制应与当前表强绑定
- 详情轨保持 sticky，但首屏内容要克制，优先保证正文、命中线索和结果信息可快速进入视口
- 在高密度前提下保持单元格内容可扫描，避免过多嵌套卡片化

---

## Do / Don't

### Do

- 先做结构，再做样式
- 一屏只保留一个真正主按钮
- 优先复用少量稳定的布局模式（`flat-meta-strip`、`flat-row-list`、`flat-actions`），不轻易发明新容器
- 用字号、字重、留白和语义色表达层级
- 新页面只使用 `Inter + Noto Sans SC` 字体族
- 界面文案使用中文

### Don't

- 不用英文页面标题或英文 eyebrow
- 不混排等宽字体
- 不新增重复描述性说明文字
- 不用卡片套卡片建立秩序
- 不加装饰性渐变、大阴影或圆角容器
- 不写死色值，一律走语义 token

---

## 给 AI 使用的执行提示

当你在这个仓库里做前端设计或 UI 改动时，默认按以下顺序执行：

1. 先识别当前页面的主对象是谁：任务、任务包、结果、日志、设置，还是健康状态
2. 先建立阅读顺序，再决定颜色和卡片
3. 先确定唯一主操作，再安排次级和危险操作
4. 优先复用少量稳定的布局模式，不轻易发明新容器
5. 如果一个页面已经足够复杂，优先减噪，而不是继续加说明文本
6. 改完检查：中文文案、字号 / 字重 / 颜色层级、间距和控件边界、favicon 和品牌名称

默认判断标准：

- 改完后，用户应更快找到“当前是什么”“下一步点哪里”
- 页面应更像一套统一工作台，而不是几个独立 demo 页面
- 若视觉更像 Apple 官网但操作更绕，视为失败
- 若信息更清晰、操作更顺手、视觉更克制一致，则视为成功

---

## 与当前项目语义对齐

- `task pack = search_spec + rule_set + tags`
- 自动任务是“调度壳 + 绑定任务包”
- 手动搜索页编辑并运行的是“任务包草稿”
- 结果浏览是 `x_items_raw` / `x_items_curated` 双表工作台，结果中的 `tags` 是运行时继承自任务包的快照
- `中文 + 英文` 搜索语言在查询层是一条 `(lang:zh OR lang:en)` query，界面文案不要暗示会分别执行两次搜索
- 有界发布时间范围执行时按 1 小时窗口查询，界面展示执行 query 时只展开少量样例，避免长历史任务刷屏
- `workspace.json` 是轻量环境与 jobs registry，不是大工作区快照

这份规范描述的是当前 UI 语言。后续页面改版、组件调整和样式收口，都应以它为基线，而不是继续按单页临时判断。
