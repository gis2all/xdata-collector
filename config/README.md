# Config

`config/` 用于存放可维护、可版本化的静态配置文件。

## 当前内容

- `search_presets*.json`：搜索预设与查询组合

## 当前边界

- 这里只放可版本化的静态配置，不放运行日志或临时输出
- `.env` 继续放在仓库根目录，不并入 `config/`
- 业务逻辑默认值仍以 `backend/` 中的实现为准，`config/` 只承接外部化配置
